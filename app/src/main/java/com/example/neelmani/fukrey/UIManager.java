/*
package com.example.neelmani.fukrey;

import com.google.android.gms.location.LocationServices;

*/
/**
 * Created by Neelmani on 16-Aug-15.
 *//*

public class UIManager {

    public static void updateUI(String result) {

       if (null == LocationManager.currentLocation) {
            LocationManager.currentLocation = LocationServices.FusedLocationApi.getLastLocation(LocationManager.googleApiClient);
         result="LAST LOCATION - "+result;
        }
        if (null != LocationManager.currentLocation)
        {
            String lat = String.valueOf(LocationManager.currentLocation.getLatitude());
            String lng = String.valueOf(LocationManager.currentLocation.getLongitude());

            // Getting reference to TextView tv_longitude

            // Setting Current Longitude
            //  tvLongitude.setText("Longitude:" + location.getLongitude());

            // Setting Current Latitude
            //tvLatitude.setText("Latitude:" + location.getLatitude() );

          */
/*  MainActivity.tvLatitude.setText("At Time: " + LocationManager.lastUpdateTime + "\n" +
                    "Latitude: " + lat + "\n" +
                    "Longitude: " + lng + "\n" +
                    "Accuracy: " + LocationManager.currentLocation.getAccuracy() + "\n" +
                    "Provider: " + LocationManager.currentLocation.getProvider() + "\n" +
                    "reposne: " + result);*//*

        } else
        {
          //  MainActivity.tvLatitude.setText("Location can't be retrived");
        }


        }
    }


*/
